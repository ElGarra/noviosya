# Database
DATABASE_URL="postgresql://postgres:postgres@db:5432/cuanto_queda"
DIRECT_URL="postgresql://postgres:postgres@db:5432/cuanto_queda"

# App
NEXT_PUBLIC_APP_URL="https://tudominio.com"
WEDDING_SLUG="florencia-matias"

# Auth — generate with: openssl rand -base64 32
NEXTAUTH_URL="https://tudominio.com"
NEXTAUTH_SECRET=""
ADMIN_PASSWORD_HASH=""   # bcrypt hash — generate with: node -e "require('bcryptjs').hash('tu-password',10).then(console.log)"

# Email (Resend — resend.com)
RESEND_API_KEY=""
EMAIL_FROM="invitaciones@tudominio.com"
ADMIN_EMAIL=""
